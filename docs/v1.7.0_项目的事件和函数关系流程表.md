# v1.7.0 项目的事件和函数关系流程表

## 1. 版本定位

`v1.7.0` 是当前项目在完成 `Wi-Fi + HTTP + JSON` 主链之后，正式进入设备远程升级能力的第一个版本。

这一版的重点不是继续验证网络基础，而是：

```text
设备怎么检查新版本
-> 怎么拿到升级地址
-> 怎么执行 OTA
-> 怎么把 OTA 状态同步到 LCD 和串口
```

---

## 2. 总体模块关系图

```mermaid
flowchart TD
    A["main()<br/><sub>[main.c]</sub><br/><sub>（程序入口）</sub>"] --> B["app_main_task_start()<br/><sub>[app_main_task.c]</sub><br/><sub>（创建主任务）</sub>"]
    B --> C["app_main_task()<br/><sub>[app_main_task.c]</sub><br/><sub>（系统初始化与主循环）</sub>"]

    C --> D["wifi_service<br/><sub>[wifi_service.c]</sub><br/><sub>（联网状态管理）</sub>"]
    C --> E["http_service<br/><sub>[http_service.c]</sub><br/><sub>（版本信息请求）</sub>"]
    C --> F["ota_service<br/><sub>[ota_service.c]</sub><br/><sub>（OTA 升级服务）</sub>"]
    C --> G["display_service<br/><sub>[display_service.c]</sub><br/><sub>（LCD 状态输出）</sub>"]
    C --> H["app_event_task<br/><sub>[app_event_task.c]</sub><br/><sub>（统一事件处理）</sub>"]

    D --> I["ESP-IDF Wi-Fi / IP Event<br/><sub>（底层联网事件）</sub>"]
    E --> J["Version JSON<br/><sub>（版本信息接口）</sub>"]
    F --> K["OTA BIN<br/><sub>（固件升级包）</sub>"]
    F --> L["app_update / OTA API<br/><sub>（升级执行）</sub>"]

    F --> G
    F --> M["串口日志"]
```

---

## 3. 总体初始化流程图

```mermaid
flowchart TD
    A["main()<br/><sub>[main.c]</sub>"] --> B["app_main_task_start()<br/><sub>[app_main_task.c]</sub>"]
    B --> C["app_main_task()<br/><sub>[app_main_task.c]</sub>"]

    C --> D["xQueueCreate()<br/><sub>[app_main_task.c]</sub><br/><sub>事件队列</sub>"]
    D --> E["led_service_init()<br/><sub>[led_service.c]</sub>"]
    E --> F["beep_service_init()<br/><sub>[beep_service.c]</sub>"]
    F --> G["display_service_init()<br/><sub>[display_service.c]</sub>"]
    G --> H["wifi_service_init()<br/><sub>[wifi_service.c]</sub>"]
    H --> I["http_service_init()<br/><sub>[http_service.c]</sub>"]
    I --> J["ota_service_init()<br/><sub>[ota_service.c]</sub>"]
    J --> K["app_event_task_start()<br/><sub>[app_event_task.c]</sub>"]
    K --> L["button_service_init()<br/><sub>[button_service.c]</sub>"]
    L --> M["wifi_service_start()<br/><sub>[wifi_service.c]</sub>"]
    M --> N["主循环 while(1)<br/><sub>[app_main_task.c]</sub>"]
```

---

## 4. 初始化依赖关系图

```mermaid
flowchart TD
    A["app_main_task()<br/><sub>[app_main_task.c]</sub><br/><sub>（统一组织初始化顺序）</sub>"] --> B["display_service_init()<br/><sub>[display_service.c]</sub>"]
    A --> C["wifi_service_init()<br/><sub>[wifi_service.c]</sub>"]
    A --> D["http_service_init()<br/><sub>[http_service.c]</sub>"]
    A --> E["ota_service_init()<br/><sub>[ota_service.c]</sub>"]

    B --> F["bsp_lcd_init()<br/><sub>[bsp_lcd.c]</sub>"]
    F --> G["lcd_st7789v_init()<br/><sub>[lcd_st7789v.c]</sub>"]
    G --> H["spi_bus_init()<br/><sub>[spi_bus.c]</sub>"]

    C --> I["esp_netif_init()<br/><sub>[wifi_service.c]</sub>"]
    C --> J["esp_event_loop_create_default()<br/><sub>[wifi_service.c]</sub>"]
    C --> K["esp_wifi_init()<br/><sub>[wifi_service.c]</sub>"]

    D --> L["依赖 wifi_service 已具备联网前提<br/><sub>（逻辑依赖）</sub>"]
    E --> M["依赖 wifi_service + http_service<br/><sub>（网络与版本信息前提）</sub>"]
```

---

## 5. OTA 主流程图

```mermaid
flowchart TD
    A["wifi_service_get_state()<br/><sub>[wifi_service.c]</sub><br/><sub>（判断是否 GOT_IP）</sub>"] --> B{"网络是否已就绪"}
    B -->|否| C["等待或返回<br/><sub>（不做 OTA）</sub>"]
    B -->|是| D["http_service_request_get()<br/><sub>[http_service.c]</sub><br/><sub>（请求版本信息）</sub>"]
    D --> E["json_parse_version_info()<br/><sub>[ota_service.c]</sub><br/><sub>（解析 version/url）</sub>"]
    E --> F{"是否有新版本"}
    F -->|否| G["ota_service_set_state(IDLE/NO_UPDATE)<br/><sub>[ota_service.c]</sub>"]
    F -->|是| H["ota_service_begin()<br/><sub>[ota_service.c]</sub><br/><sub>（开始 OTA）</sub>"]
    H --> I["esp_ota_begin()<br/><sub>（打开 OTA 写入）</sub>"]
    I --> J["下载固件并写入 OTA 分区<br/><sub>[ota_service.c]</sub>"]
    J --> K["esp_ota_end()<br/><sub>（完成写入）</sub>"]
    K --> L["esp_ota_set_boot_partition()<br/><sub>（设置下次启动分区）</sub>"]
    L --> M["ota_service_set_state(SUCCESS)<br/><sub>[ota_service.c]</sub>"]
    M --> N["esp_restart()<br/><sub>（重启进入新版本）</sub>"]
```

---

## 6. 版本检查流程图

```mermaid
flowchart TD
    A["http_service_request_get()<br/><sub>[http_service.c]</sub><br/><sub>（请求版本 JSON）</sub>"] --> B["response_body<br/><sub>（版本描述文本）</sub>"]
    B --> C["ota_service_parse_version_info()<br/><sub>[ota_service.c]</sub><br/><sub>（解析 version/url）</sub>"]
    C --> D["current_version<br/><sub>（当前固件版本）</sub>"]
    C --> E["server_version<br/><sub>（服务器版本）</sub>"]
    C --> F["firmware_url<br/><sub>（升级包地址）</sub>"]
    D --> G["ota_service_compare_version()<br/><sub>[ota_service.c]</sub>"]
    E --> G
    G --> H{"是否需要升级"}
    H -->|否| I["显示 NO_UPDATE"]
    H -->|是| J["进入 OTA 下载流程"]
```

---

## 7. OTA 状态流图

```mermaid
flowchart LR
    A["IDLE"] --> B["CHECK"]
    B --> C["READY"]
    C --> D["DOWNLOADING"]
    D --> E["SUCCESS"]
    D --> F["FAIL"]
    B --> F
    C --> F
```

---

## 8. 关键参数传递图

```mermaid
flowchart TD
    A["http_service_request_get()<br/><sub>[http_service.c]</sub><br/><sub>（url 版本接口地址）</sub>"] --> B["ota_service_parse_version_info()<br/><sub>[ota_service.c]</sub><br/><sub>（json_text 版本描述文本）</sub>"]
    B --> C["ota_service_compare_version()<br/><sub>[ota_service.c]</sub><br/><sub>（current_version 当前版本）（server_version 服务器版本）</sub>"]
    C --> D["ota_service_begin()<br/><sub>[ota_service.c]</sub><br/><sub>（firmware_url 固件地址）</sub>"]
    D --> E["display_service_show_ota_status()<br/><sub>[display_service.c]</sub><br/><sub>（state OTA状态）（message 提示文本）</sub>"]
```

---

## 9. OTA 与 LCD 联动流程图

```mermaid
flowchart TD
    A["ota_service 更新状态"] --> B["display_service_show_ota_status()<br/><sub>[display_service.c]</sub>"]
    B --> C["ota_panel_dirty = true"]
    C --> D["display_service_process()<br/><sub>[display_service.c]</sub>"]
    D --> E["display_service_draw_ota_panel()<br/><sub>[display_service.c]</sub>"]
    E --> F["LCD 显示 OTA : 状态 / 提示"]
```

---

## 10. 主循环推进图

```mermaid
flowchart TD
    A["app_main_task while(1)<br/><sub>[app_main_task.c]</sub>"] --> B["button_service_process()<br/><sub>按键状态机推进</sub>"]
    B --> C["led_service_process()<br/><sub>LED 灯效推进</sub>"]
    C --> D["beep_service_process()<br/><sub>蜂鸣器时序推进</sub>"]
    D --> E["wifi_service_process()<br/><sub>Wi-Fi 信息刷新</sub>"]
    E --> F["http_service_process()<br/><sub>版本信息请求推进</sub>"]
    F --> G["ota_service_process()<br/><sub>OTA 状态推进</sub>"]
    G --> H["display_service_process()<br/><sub>LCD 脏区刷新</sub>"]
    H --> I["vTaskDelay()<br/><sub>让出 CPU</sub>"]
```

---

## 11. 页面布局建议图

```mermaid
flowchart TD
    A["LCD 首页"] --> B["标题区<br/><sub>项目名</sub>"]
    A --> C["项目信息区<br/><sub>版本 / 阶段</sub>"]
    A --> D["LED 状态区"]
    A --> E["BEEP 状态区"]
    A --> F["Wi-Fi 状态区"]
    A --> G["HTTP 状态区"]
    A --> H["OTA 状态区<br/><sub>OTA / MESSAGE</sub>"]
    A --> I["最近事件区"]
```

---

## 12. 当前版本最值得理解的运行逻辑

这版最核心的理解是：

```text
Wi-Fi 负责“网络前提”
-> HTTP 负责“拿版本信息”
-> OTA 负责“下载并切换固件”
-> LCD 和日志负责“把升级状态表达出来”
```

建议后面看代码时按这个顺序理解：

1. 先看 `wifi_service`
2. 再看 `http_service`
3. 再看 `ota_service`
4. 最后看 `display_service`
