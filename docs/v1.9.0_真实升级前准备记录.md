# v1.9.0 真实升级前准备记录

## 1. 当前阶段

当前 `v1.9.0` 已完成：

- 双 OTA 分区切换
- 真实 OTA 下载与写入主链接入
- OTA 状态扩展到 `VERIFY`
- 编译通过

当前还未真正完成的最后闭环是：

- 准备真实固件下载地址
- 开启自动升级
- 上板验证真实升级成功

---

## 2. 当前固件文件

当前构建产物：

- `build/codex_project_tep.bin`

当前文件大小约：

- `1033152` 字节

---

## 3. 当前版本接口状态

当前腾讯云版本接口已经可正常返回：

- `version`
- `url`
- `message`

当前版本接口地址：

- `http://1406834977-7btxur47w5.ap-guangzhou.tencentscf.com/version`

---

## 4. 当前仍需处理的问题

当前版本 JSON 中的：

- `url`

如果仍是占位地址，就只能做到：

- `READY`

还不能真正完成：

- `DOWNLOADING -> VERIFY -> SUCCESS`

---

## 5. 下一步准备项

### 5.1 固件文件重命名

建议把当前固件复制成带版本号名字，例如：

- `codex_project_tep_v1.9.1.bin`

### 5.2 上传到公网可访问地址

建议优先：

- 腾讯云 COS

### 5.3 更新腾讯云版本接口 JSON

把：

- `url`

替换成真实 bin 地址。

### 5.4 修改自动升级开关

把：

- `APP_OTA_AUTO_UPGRADE`

从：

- `0`

改成：

- `1`

### 5.5 上板验证真实升级

重点观察：

- `DOWNLOADING`
- `VERIFY`
- `SUCCESS`
- `REBOOTING`
- 重启后版本号是否更新

---

## 6. 当前推荐主线

```text
build/codex_project_tep.bin
-> 改成带版本号的 bin
-> 上传到腾讯云 COS
-> 更新腾讯云版本接口里的 url
-> APP_OTA_AUTO_UPGRADE 改为 1
-> 重新编译烧录
-> 上板做真实 OTA 升级验证
```
