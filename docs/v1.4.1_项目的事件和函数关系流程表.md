# v1.4.1 项目的事件和函数关系流程表

## 1. 版本定位

`v1.4.1` 是 `v1.4.0` 的显示模板收口版。  
这一版不再新增硬件，而是继续把已经跑通的：

- `spi_bus`
- `lcd_st7789v`
- `bsp_lcd`
- `display_service`

这条显示链整理得更适合后续复用。

重点不是“显示更多东西”，而是：

```text
让显示服务的页面结构、函数结构、初始化关系更清楚
```

---

## 2. 总体模块关系图

```mermaid
flowchart TD
    A["main()<br/><sub>[main.c]</sub><br/><sub>（程序入口）</sub>"] --> B["app_main_task_start()<br/><sub>[app_main_task.c]</sub><br/><sub>（创建主任务）</sub>"]
    B --> C["app_main_task()<br/><sub>[app_main_task.c]</sub><br/><sub>（主初始化与主循环）</sub>"]

    C --> D["button_service<br/><sub>[button_service.c]</sub><br/><sub>（按键状态机）</sub>"]
    C --> E["app_event_task<br/><sub>[app_event_task.c]</sub><br/><sub>（统一事件处理）</sub>"]
    C --> F["led_service<br/><sub>[led_service.c]</sub><br/><sub>（LED 服务）</sub>"]
    C --> G["beep_service<br/><sub>[beep_service.c]</sub><br/><sub>（蜂鸣器服务）</sub>"]
    C --> H["display_service<br/><sub>[display_service.c]</sub><br/><sub>（显示服务）</sub>"]

    H --> I["bsp_lcd<br/><sub>[bsp_lcd.c]</sub><br/><sub>（板级 LCD 适配）</sub>"]
    I --> J["lcd_st7789v<br/><sub>[lcd_st7789v.c]</sub><br/><sub>（LCD 驱动）</sub>"]
    J --> K["spi_bus<br/><sub>[spi_bus.c]</sub><br/><sub>（SPI 总线模板）</sub>"]

    D --> L["bsp_button / bsp_xl9555<br/><sub>[bsp_button.c / bsp_xl9555.c]</sub><br/><sub>（板载按键输入）</sub>"]
    G --> M["bsp_xl9555<br/><sub>[bsp_xl9555.c]</sub><br/><sub>（蜂鸣器输出）</sub>"]
    I --> M
    M --> N["xl9555 + i2c_bus<br/><sub>[xl9555.c / i2c_bus.c]</sub><br/><sub>（XL9555 与 I2C 驱动）</sub>"]
```

---

## 3. 总体初始化流程图

```mermaid
flowchart TD
    A["main()<br/><sub>[main.c]</sub>"] --> B["app_main_task_start()<br/><sub>[app_main_task.c]</sub>"]
    B --> C["app_main_task()<br/><sub>[app_main_task.c]</sub>"]

    C --> D["xQueueCreate()<br/><sub>[app_main_task.c]</sub><br/><sub>（event_queue 事件队列）</sub>"]
    D --> E["led_service_init()<br/><sub>[led_service.c]</sub>"]
    E --> F["beep_service_init()<br/><sub>[beep_service.c]</sub>"]
    F --> G["display_service_init()<br/><sub>[display_service.c]</sub>"]
    G --> H["bsp_lcd_init()<br/><sub>[bsp_lcd.c]</sub>"]
    H --> I["bsp_xl9555_init()<br/><sub>[bsp_xl9555.c]</sub>"]
    I --> J["bsp_xl9555_lcd_ctrl_init()<br/><sub>[bsp_xl9555.c]</sub>"]
    J --> K["spi_bus_init()<br/><sub>[spi_bus.c]</sub>"]
    K --> L["spi_bus_register_device()<br/><sub>[spi_bus.c]</sub>"]
    L --> M["lcd_st7789v_init()<br/><sub>[lcd_st7789v.c]</sub>"]
    M --> N["display_service_refresh_home()<br/><sub>[display_service.c]</sub>"]
    N --> O["app_event_task_start(event_queue)<br/><sub>[app_event_task.c]</sub>"]
    O --> P["button_service_init(event_queue)<br/><sub>[button_service.c]</sub>"]
    P --> Q["主循环 while(1)<br/><sub>[app_main_task.c]</sub>"]
```

---

## 4. 初始化依赖关系图

```mermaid
flowchart TD
    A["app_main_task()<br/><sub>[app_main_task.c]</sub><br/><sub>（统一组织启动顺序）</sub>"] --> B["beep_service_init()<br/><sub>[beep_service.c]</sub>"]
    A --> C["display_service_init()<br/><sub>[display_service.c]</sub>"]
    A --> D["button_service_init()<br/><sub>[button_service.c]</sub>"]

    B --> E["bsp_xl9555_init()<br/><sub>[bsp_xl9555.c]</sub><br/><sub>（蜂鸣器依赖 XL9555）</sub>"]
    C --> F["bsp_lcd_init()<br/><sub>[bsp_lcd.c]</sub><br/><sub>（显示依赖 LCD 板级层）</sub>"]
    D --> G["bsp_button_init()<br/><sub>[bsp_button.c]</sub><br/><sub>（按键依赖 XL9555 输入）</sub>"]

    F --> E
    G --> E

    E --> H["xl9555_init()<br/><sub>[xl9555.c]</sub><br/><sub>（XL9555 驱动入口）</sub>"]
    H --> I["i2c_bus_init()<br/><sub>[i2c_bus.c]</sub><br/><sub>（XL9555 依赖 I2C）</sub>"]

    F --> J["spi_bus_init()<br/><sub>[spi_bus.c]</sub><br/><sub>（LCD 依赖 SPI bus）</sub>"]
    J --> K["spi_bus_register_device()<br/><sub>[spi_bus.c]</sub><br/><sub>（注册 LCD 设备）</sub>"]
    K --> L["lcd_st7789v_init()<br/><sub>[lcd_st7789v.c]</sub><br/><sub>（初始化屏幕芯片）</sub>"]
```

说明：

- 当前项目采用的是“上层统一组织初始化 + 下层模块保留依赖自检”的混合方式
- 这样既能看清主启动顺序，也方便后续单独复用某个模块

---

## 5. 主循环推进图

```mermaid
flowchart TD
    A["app_main_task 主循环<br/><sub>[app_main_task.c]</sub>"] --> B["led_service_process()<br/><sub>[led_service.c]</sub><br/><sub>（推进 LED 闪烁）</sub>"]
    B --> C["beep_service_process()<br/><sub>[beep_service.c]</sub><br/><sub>（推进蜂鸣器时序）</sub>"]
    C --> D["button_service_process()<br/><sub>[button_service.c]</sub><br/><sub>（推进按键状态机）</sub>"]
    D --> E["display_service_process()<br/><sub>[display_service.c]</sub><br/><sub>（触发页面刷新）</sub>"]
    E --> F["vTaskDelay()<br/><sub>[app_main_task.c]</sub><br/><sub>（主循环节拍）</sub>"]
    F --> A
```

---

## 6. 总体事件流

```mermaid
flowchart TD
    A["板载按键 KEY0~KEY3<br/><sub>[bsp_button.c]</sub>"] --> B["button_service_process()<br/><sub>[button_service.c]</sub><br/><sub>（识别 SHORT/LONG/DOUBLE）</sub>"]
    B --> C["button_service_publish_event()<br/><sub>[button_service.c]</sub><br/><sub>（组装 app_event_msg_t）</sub>"]
    C --> D["Queue<br/><sub>[FreeRTOS]</sub><br/><sub>（统一事件队列）</sub>"]
    D --> E["app_event_task()<br/><sub>[app_event_task.c]</sub><br/><sub>（消费统一事件）</sub>"]

    E --> F["led_service_set_mode()<br/><sub>[led_service.c]</sub>"]
    E --> G["beep_service_play() / force()<br/><sub>[beep_service.c]</sub>"]
    E --> H["display_service_show_xxx()<br/><sub>[display_service.c]</sub><br/><sub>（更新显示缓存）</sub>"]
```

---

## 7. 统一事件消息与关键参数传递

### 7.1 消息结构图

```mermaid
flowchart TD
    A["app_event_msg_t<br/><sub>[app_types.h]</sub>"] --> B["source<br/><sub>（事件来源）</sub>"]
    A --> C["type<br/><sub>（事件类型）</sub>"]
    A --> D["param1<br/><sub>（button_id 等主参数）</sub>"]
    A --> E["param2<br/><sub>（button_event 等次参数）</sub>"]
    A --> F["timestamp_ms<br/><sub>（时间戳）</sub>"]
```

### 7.2 参数传递总图

```mermaid
flowchart TD
    A["button_service_handle_event()<br/><sub>[button_service.c]</sub><br/><sub>（btn_id 按键编号）</sub><br/><sub>（event 按键事件类型）</sub>"] --> B["button_service_publish_event()<br/><sub>[button_service.c]</sub><br/><sub>（btn_id）</sub><br/><sub>（event）</sub><br/><sub>（now_ms 当前时间）</sub>"]
    B --> C["app_event_msg_t<br/><sub>[app_types.h]</sub><br/><sub>（param1 = btn_id）</sub><br/><sub>（param2 = event）</sub><br/><sub>（timestamp_ms = now_ms）</sub>"]
    C --> D["xQueueSend()<br/><sub>[FreeRTOS]</sub><br/><sub>（发送统一事件）</sub>"]
    D --> E["xQueueReceive()<br/><sub>[app_event_task.c]</sub><br/><sub>（接收统一事件）</sub>"]
    E --> F["app_event_handle_button_message()<br/><sub>[app_event_task.c]</sub><br/><sub>（button_id = param1）</sub><br/><sub>（button_event = param2）</sub>"]
    F --> G["display_service_show_last_button()<br/><sub>[display_service.c]</sub><br/><sub>（button_id / button_event）</sub>"]
    F --> H["display_service_show_led_status()<br/><sub>[display_service.c]</sub><br/><sub>（led_id / next_mode）</sub>"]
    F --> I["display_service_show_beep_status()<br/><sub>[display_service.c]</sub><br/><sub>（enabled / test_mode）</sub>"]
```

---

## 8. LCD 显示模板主链

```mermaid
flowchart TD
    A["display_service_init()<br/><sub>[display_service.c]</sub><br/><sub>（显示服务入口）</sub>"] --> B["bsp_lcd_init()<br/><sub>[bsp_lcd.c]</sub><br/><sub>（板级 LCD 初始化）</sub>"]
    B --> C["spi_bus_init()<br/><sub>[spi_bus.c]</sub><br/><sub>（SPI 总线初始化）</sub>"]
    C --> D["spi_bus_register_device()<br/><sub>[spi_bus.c]</sub><br/><sub>（注册 LCD 设备）</sub>"]
    D --> E["lcd_st7789v_init()<br/><sub>[lcd_st7789v.c]</sub><br/><sub>（发送初始化序列）</sub>"]
    E --> F["display_service_refresh_home()<br/><sub>[display_service.c]</sub><br/><sub>（绘制首页）</sub>"]
```

---

## 9. display_service 页面结构图

这一块是 `v1.4.1` 最核心的新增理解视角。  
它不只是看函数调用，而是看“页面被拆成了哪几块”。

```mermaid
flowchart TD
    A["display_service_refresh_home()<br/><sub>[display_service.c]</sub><br/><sub>（首页总刷新入口）</sub>"] --> B["display_service_draw_header()<br/><sub>[display_service.c]</sub><br/><sub>（标题区）</sub>"]
    A --> C["display_service_draw_project_info()<br/><sub>[display_service.c]</sub><br/><sub>（版本与阶段区）</sub>"]
    A --> D["display_service_draw_led_panel()<br/><sub>[display_service.c]</sub><br/><sub>（LED 状态区）</sub>"]
    A --> E["display_service_draw_beep_panel()<br/><sub>[display_service.c]</sub><br/><sub>（蜂鸣器状态区）</sub>"]
    A --> F["display_service_draw_last_event_panel()<br/><sub>[display_service.c]</sub><br/><sub>（最近事件区）</sub>"]
```

说明：

- 这张图体现的是 `v1.4.1` 最想优化的方向
- 后面不管显示内容怎么增加，建议都优先往“按区域拆函数”的方式发展

---

## 10. display_service 函数关系图

```mermaid
flowchart TD
    A["display_service_init()<br/><sub>[display_service.c]</sub><br/><sub>（初始化显示服务）</sub>"] --> B["bsp_lcd_init()<br/><sub>[bsp_lcd.c]</sub>"]
    A --> C["display_service_refresh_home()<br/><sub>[display_service.c]</sub>"]

    D["display_service_show_version()<br/><sub>[display_service.c]</sub><br/><sub>（version 版本号）</sub>"] --> E["s_display.version<br/><sub>（更新显示缓存）</sub>"]
    F["display_service_show_stage()<br/><sub>[display_service.c]</sub><br/><sub>（stage 阶段名）</sub>"] --> G["s_display.stage<br/><sub>（更新显示缓存）</sub>"]
    H["display_service_show_led_status()<br/><sub>[display_service.c]</sub><br/><sub>（led_id / mode）</sub>"] --> I["s_display.led_modes[]<br/><sub>（更新LED缓存）</sub>"]
    J["display_service_show_beep_status()<br/><sub>[display_service.c]</sub><br/><sub>（enabled / test_mode）</sub>"] --> K["s_display.beep_enabled / beep_test_mode<br/><sub>（更新蜂鸣器缓存）</sub>"]
    L["display_service_show_last_button()<br/><sub>[display_service.c]</sub><br/><sub>（button_id / button_event）</sub>"] --> M["s_display.last_button_xxx<br/><sub>（更新事件缓存）</sub>"]

    N["display_service_process()<br/><sub>[display_service.c]</sub><br/><sub>（周期处理）</sub>"] --> O["dirty ?<br/><sub>（是否需要刷新）</sub>"]
    O -->|是| C
```

---

## 11. SPI 通用驱动流程图

```mermaid
flowchart TD
    A["spi_bus_init()<br/><sub>[spi_bus.c]</sub><br/><sub>（host / mosi / miso / sclk）</sub>"] --> B["spi_bus_register_device()<br/><sub>[spi_bus.c]</sub><br/><sub>（cs_gpio / clock_hz）</sub>"]
    B --> C["spi_bus_transmit()<br/><sub>[spi_bus.c]</sub><br/><sub>（tx_data / length）</sub>"]
    B --> D["spi_bus_transmit_cmd()<br/><sub>[spi_bus.c]</sub><br/><sub>（cmd 命令值）</sub>"]
    B --> E["spi_bus_transmit_data()<br/><sub>[spi_bus.c]</sub><br/><sub>（data 数据缓存）</sub>"]
```

---

## 12. LCD_ST7789V 驱动流程图

```mermaid
flowchart TD
    A["lcd_st7789v_init()<br/><sub>[lcd_st7789v.c]</sub><br/><sub>（初始化序列）</sub>"] --> B["lcd_st7789v_write_cmd()<br/><sub>[lcd_st7789v.c]</sub><br/><sub>（写 LCD 命令）</sub>"]
    B --> C["lcd_st7789v_write_data()<br/><sub>[lcd_st7789v.c]</sub><br/><sub>（写 LCD 数据）</sub>"]
    C --> D["lcd_st7789v_set_window()<br/><sub>[lcd_st7789v.c]</sub><br/><sub>（设置绘图区域）</sub>"]
    D --> E["lcd_st7789v_fill_rect()<br/><sub>[lcd_st7789v.c]</sub><br/><sub>（区域填充）</sub>"]
    D --> F["lcd_st7789v_draw_char_scaled()<br/><sub>[lcd_st7789v.c]</sub><br/><sub>（画单字符）</sub>"]
    F --> G["lcd_st7789v_draw_string_scaled()<br/><sub>[lcd_st7789v.c]</sub><br/><sub>（画字符串）</sub>"]
```

---

## 13. app_event_task 与显示联动流程图

```mermaid
flowchart TD
    A["app_event_task()<br/><sub>[app_event_task.c]</sub><br/><sub>（接收统一事件）</sub>"] --> B["app_event_handle_button_message()<br/><sub>[app_event_task.c]</sub><br/><sub>（解析 button_id / button_event）</sub>"]
    B --> C["业务键分支<br/><sub>（BTN_SYS~BTN_ERR）</sub>"]
    B --> D["功能键分支<br/><sub>（BTN_FUNC）</sub>"]

    C --> E["led_service_set_mode()<br/><sub>[led_service.c]</sub>"]
    C --> F["beep_service_play()<br/><sub>[beep_service.c]</sub>"]
    C --> G["display_service_show_last_button()<br/><sub>[display_service.c]</sub>"]
    C --> H["display_service_show_led_status()<br/><sub>[display_service.c]</sub>"]

    D --> I["beep_service_set_enabled()<br/><sub>[beep_service.c]</sub>"]
    D --> J["beep_service_set_test_mode()<br/><sub>[beep_service.c]</sub>"]
    D --> K["display_service_show_beep_status()<br/><sub>[display_service.c]</sub>"]
    D --> L["display_service_show_last_button()<br/><sub>[display_service.c]</sub>"]
```

---

## 14. 页面布局参数图

这张图不是调用链，而是帮助后面整理页面布局常量。

```mermaid
flowchart TD
    A["display layout config<br/><sub>[display_service.c]</sub>"] --> B["标题区坐标<br/><sub>（title_x / title_y）</sub>"]
    A --> C["项目区坐标<br/><sub>（info_x / info_y）</sub>"]
    A --> D["LED区坐标<br/><sub>（led_x / led_y）</sub>"]
    A --> E["蜂鸣器区坐标<br/><sub>（beep_x / beep_y）</sub>"]
    A --> F["事件区坐标<br/><sub>（event_x / event_y）</sub>"]
    A --> G["字号配置<br/><sub>（title_scale / body_scale / small_scale）</sub>"]
    A --> H["颜色配置<br/><sub>（title_color / led_color / beep_color / event_color）</sub>"]
```

说明：

- `v1.4.1` 很适合把这些布局参数集中出来
- 这样后面改页面时，不用在大段绘制逻辑里到处找数字

---

## 15. 哪些地方我觉得还值得额外补

如果后面这版继续往下细化，我觉得最值得再补的是两块：

- `lcd_st7789v` 初始化命令时序图
- 首页布局示意图

前者更偏驱动理解，后者更偏页面设计理解。

---

## 16. 推荐阅读顺序

建议后面阅读 `v1.4.1` 时按这个顺序看：

1. 总体模块关系图
2. 总体初始化流程图
3. 初始化依赖关系图
4. 主循环推进图
5. 总体事件流
6. LCD 显示模板主链
7. display_service 页面结构图
8. display_service 函数关系图
9. 页面布局参数图
10. 最后再回到具体源码

这样会最容易把“显示模板”和“系统运行逻辑”对齐起来。
