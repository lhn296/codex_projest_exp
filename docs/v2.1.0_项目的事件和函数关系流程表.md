# v2.1.0 项目的事件和函数关系流程表

## 总体主链

```mermaid
flowchart TD
    A[config_service] --> B[wifi_service]
    A --> C[http_service]
    A --> D[ota_service]
    E[config_cli_service] --> A
    E --> C
    E --> D
    A --> F[display_service]
    E --> F
```

## 初始化顺序

```mermaid
flowchart TD
    A[app_main_task] --> B[display_service_init]
    B --> C[config_service_init]
    C --> D[config_service_load]
    D --> E[config_service_self_test]
    E --> F[config_cli_service_init]
    F --> G[wifi_service_init]
    G --> H[http_service_init]
    H --> I[ota_service_init]
```

## 配置来源显示链

```mermaid
flowchart LR
    A[config_service_get_source_summary] --> B[display_service_show_config_source]
    B --> C[display_service_draw_project_info]
    C --> D[LCD CFG line]
```

## 串口命令执行链

```mermaid
flowchart LR
    A[monitor input] --> B[config_cli_service_process]
    B --> C[config_cli_service_execute_line]
    C --> D[sub command]
```

## cfg status

```mermaid
flowchart LR
    A[cfg status] --> B[print current config]
    B --> C[print config source]
    C --> D[print http and ota status]
```

## cfg source

```mermaid
flowchart LR
    A[cfg source] --> B[config_service_get_wifi_source]
    A --> C[config_service_get_http_source]
    A --> D[config_service_get_ota_source]
    A --> E[config_service_get_source_summary]
```

## cfg test http

```mermaid
flowchart LR
    A[cfg test http] --> B[config_service_get]
    B --> C[http_service_request_get]
    C --> D[http_service_get_status_code]
    C --> E[http_service_get_message]
```

## cfg test ota

```mermaid
flowchart LR
    A[cfg test ota] --> B[ota_service_check_now]
    B --> C[ota_service_check_once]
    C --> D[http_service_request_get]
    D --> E[ota_service_parse_cloud_payload]
    E --> F[ota state update]
```

## 关键函数关系

### 配置层

- `config_service_init()`
- `config_service_load()`
- `config_service_save()`
- `config_service_reset_to_default()`
- `config_service_get_source_summary()`

### 显示层

- `display_service_show_config_source()`
- `display_service_draw_project_info()`

### CLI 层

- `config_cli_service_init()`
- `config_cli_service_process()`
- `config_cli_service_execute_line()`
- `config_cli_service_log_status()`
- `config_cli_service_log_config_source()`

### OTA 层

- `ota_service_process()`
- `ota_service_check_now()`
- `ota_service_check_once()`
