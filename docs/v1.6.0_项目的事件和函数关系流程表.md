# v1.6.0 项目的事件和函数关系流程表

## 1. 版本定位

`v1.6.0` 是当前项目在 `Wi-Fi` 联网跑通之后，正式进入网络应用层的第一个版本。  
这一版的重点不是继续讨论“能不能联网”，而是：

```text
设备怎么主动发起 HTTP 请求
-> 怎么收到响应
-> 怎么解析 JSON
-> 怎么把结果同步到 LCD 和串口
```

---

## 2. 总体模块关系图

```mermaid
flowchart TD
    A["main()<br/><sub>[main.c]</sub><br/><sub>（程序入口）</sub>"] --> B["app_main_task_start()<br/><sub>[app_main_task.c]</sub><br/><sub>（创建主任务）</sub>"]
    B --> C["app_main_task()<br/><sub>[app_main_task.c]</sub><br/><sub>（系统初始化与主循环）</sub>"]

    C --> D["wifi_service<br/><sub>[wifi_service.c]</sub><br/><sub>（联网状态管理）</sub>"]
    C --> E["http_service<br/><sub>[http_service.c]</sub><br/><sub>（HTTP 请求服务）</sub>"]
    C --> F["display_service<br/><sub>[display_service.c]</sub><br/><sub>（LCD 状态输出）</sub>"]
    C --> G["app_event_task<br/><sub>[app_event_task.c]</sub><br/><sub>（统一事件处理）</sub>"]

    D --> H["ESP-IDF Wi-Fi / IP Event<br/><sub>（底层联网事件）</sub>"]
    E --> I["esp_http_client<br/><sub>（HTTP 客户端）</sub>"]
    E --> J["JSON Parse<br/><sub>（响应数据解析）</sub>"]

    J --> F
    D --> F
    D --> K["串口日志"]
    E --> K
```

---

## 3. 总体初始化流程图

```mermaid
flowchart TD
    A["main()<br/><sub>[main.c]</sub>"] --> B["app_main_task_start()<br/><sub>[app_main_task.c]</sub>"]
    B --> C["app_main_task()<br/><sub>[app_main_task.c]</sub>"]

    C --> D["xQueueCreate()<br/><sub>[app_main_task.c]</sub><br/><sub>事件队列</sub>"]
    D --> E["led_service_init()<br/><sub>[led_service.c]</sub>"]
    E --> F["beep_service_init()<br/><sub>[beep_service.c]</sub>"]
    F --> G["display_service_init()<br/><sub>[display_service.c]</sub>"]
    G --> H["wifi_service_init()<br/><sub>[wifi_service.c]</sub>"]
    H --> I["http_service_init()<br/><sub>[http_service.c]</sub>"]
    I --> J["app_event_task_start()<br/><sub>[app_event_task.c]</sub>"]
    J --> K["button_service_init()<br/><sub>[button_service.c]</sub>"]
    K --> L["wifi_service_start()<br/><sub>[wifi_service.c]</sub>"]
    L --> M["主循环 while(1)<br/><sub>[app_main_task.c]</sub>"]
```

---

## 4. 初始化依赖关系图

```mermaid
flowchart TD
    A["app_main_task()<br/><sub>[app_main_task.c]</sub><br/><sub>（统一组织初始化顺序）</sub>"] --> B["display_service_init()<br/><sub>[display_service.c]</sub>"]
    A --> C["wifi_service_init()<br/><sub>[wifi_service.c]</sub>"]
    A --> D["http_service_init()<br/><sub>[http_service.c]</sub>"]

    B --> E["bsp_lcd_init()<br/><sub>[bsp_lcd.c]</sub>"]
    E --> F["lcd_st7789v_init()<br/><sub>[lcd_st7789v.c]</sub>"]
    F --> G["spi_bus_init()<br/><sub>[spi_bus.c]</sub>"]

    C --> H["wifi_service_init_nvs()<br/><sub>[wifi_service.c]</sub>"]
    C --> I["esp_netif_init()<br/><sub>[wifi_service.c]</sub>"]
    C --> J["esp_event_loop_create_default()<br/><sub>[wifi_service.c]</sub>"]
    C --> K["esp_wifi_init()<br/><sub>[wifi_service.c]</sub>"]
    C --> L["esp_event_handler_register()<br/><sub>[wifi_service.c]</sub>"]

    D --> M["依赖 wifi_service 已具备联网前提<br/><sub>（逻辑依赖）</sub>"]
```

---

## 5. HTTP 主流程图

```mermaid
flowchart TD
    A["wifi_service_get_state()<br/><sub>[wifi_service.c]</sub><br/><sub>（判断是否 GOT_IP）</sub>"] --> B{"网络是否已就绪"}
    B -->|否| C["等待或返回<br/><sub>（不发 HTTP）</sub>"]
    B -->|是| D["http_service_request_get()<br/><sub>[http_service.c]</sub>"]
    D --> E["esp_http_client_init()<br/><sub>（创建客户端）</sub>"]
    E --> F["esp_http_client_perform()<br/><sub>（发起请求）</sub>"]
    F --> G{"请求是否成功"}
    G -->|否| H["记录失败状态<br/><sub>同步日志和 LCD</sub>"]
    G -->|是| I["读取响应 Body<br/><sub>[http_service.c]</sub>"]
    I --> J["json_parse_xxx()<br/><sub>[http_service.c / json 模块]</sub>"]
    J --> K["更新结果缓存<br/><sub>code / message / data</sub>"]
    K --> L["display_service_show_http_result()<br/><sub>[display_service.c]</sub>"]
    K --> M["ESP_LOGI()<br/><sub>串口打印请求结果</sub>"]
```

---

## 6. Wi-Fi 与 HTTP 的关系图

```mermaid
flowchart LR
    A["wifi_service<br/><sub>[wifi_service.c]</sub>"] --> B["WIFI_STATE_GOT_IP<br/><sub>网络真正可用</sub>"]
    B --> C["http_service<br/><sub>[http_service.c]</sub>"]
    C --> D["HTTP Response"]
    D --> E["JSON Parse"]
    E --> F["display_service<br/><sub>[display_service.c]</sub>"]
    E --> G["串口日志"]
```

---

## 7. HTTP 请求链函数关系图

```mermaid
flowchart TD
    A["http_service_init()<br/><sub>[http_service.c]</sub><br/><sub>（初始化 HTTP 服务）</sub>"] --> B["http_service_request_get()<br/><sub>[http_service.c]</sub><br/><sub>（发起 GET 请求）</sub>"]
    B --> C["http_service_prepare_config()<br/><sub>[http_service.c]</sub><br/><sub>（准备 URL/超时等配置）</sub>"]
    C --> D["esp_http_client_init()<br/><sub>（创建客户端句柄）</sub>"]
    D --> E["esp_http_client_perform()<br/><sub>（执行请求）</sub>"]
    E --> F["http_service_read_response()<br/><sub>[http_service.c]</sub><br/><sub>（读取响应）</sub>"]
    F --> G["http_service_parse_json()<br/><sub>[http_service.c]</sub><br/><sub>（解析 JSON）</sub>"]
    G --> H["http_service_update_result()<br/><sub>[http_service.c]</sub><br/><sub>（更新结果缓存）</sub>"]
```

---

## 8. 关键参数传递图

```mermaid
flowchart TD
    A["http_service_request_get()<br/><sub>[http_service.c]</sub><br/><sub>（url 请求地址）（timeout_ms 超时时间）</sub>"] --> B["esp_http_client_init()<br/><sub>（config URL/timeout）</sub>"]
    B --> C["esp_http_client_perform()<br/><sub>（真正发请求）</sub>"]
    C --> D["http_service_read_response()<br/><sub>（response_body 响应正文）</sub>"]
    D --> E["http_service_parse_json()<br/><sub>（json_text JSON文本）</sub>"]
    E --> F["display_service_show_http_result()<br/><sub>[display_service.c]</sub><br/><sub>（code 响应码）（msg 提示文本）</sub>"]
```

---

## 9. LCD 联动显示流程图

```mermaid
flowchart TD
    A["http_service 解析完成"] --> B["更新 HTTP 结果缓存"]
    B --> C["display_service_show_http_result()<br/><sub>[display_service.c]</sub>"]
    C --> D["http_panel_dirty = true"]
    D --> E["display_service_process()<br/><sub>[display_service.c]</sub>"]
    E --> F["display_service_draw_http_panel()<br/><sub>[display_service.c]</sub>"]
    F --> G["LCD 显示 HTTP 状态 / CODE / MSG"]
```

---

## 10. JSON 解析理解图

```mermaid
flowchart TD
    A["HTTP Body<br/><sub>服务器返回的文本</sub>"] --> B["JSON Parse"]
    B --> C["code<br/><sub>业务结果码</sub>"]
    B --> D["message<br/><sub>结果说明</sub>"]
    B --> E["data<br/><sub>业务数据</sub>"]
```

---

## 11. 主循环推进图

```mermaid
flowchart TD
    A["app_main_task while(1)<br/><sub>[app_main_task.c]</sub>"] --> B["button_service_process()<br/><sub>按键状态机推进</sub>"]
    B --> C["led_service_process()<br/><sub>LED 灯效推进</sub>"]
    C --> D["beep_service_process()<br/><sub>蜂鸣器时序推进</sub>"]
    D --> E["wifi_service_process()<br/><sub>Wi-Fi 信息刷新</sub>"]
    E --> F["http_service_process()<br/><sub>HTTP 周期任务/结果推进</sub>"]
    F --> G["display_service_process()<br/><sub>LCD 脏区刷新</sub>"]
    G --> H["vTaskDelay()<br/><sub>让出 CPU</sub>"]
```

---

## 12. 页面布局建议图

```mermaid
flowchart TD
    A["LCD 首页"] --> B["标题区<br/><sub>项目名</sub>"]
    A --> C["项目信息区<br/><sub>版本 / 阶段</sub>"]
    A --> D["LED 状态区"]
    A --> E["BEEP 状态区"]
    A --> F["Wi-Fi 状态区"]
    A --> G["HTTP 状态区<br/><sub>HTTP / CODE / MSG</sub>"]
    A --> H["最近事件区"]
```

---

## 13. 当前版本最值得理解的运行逻辑

这版最核心的理解是：

```text
Wi-Fi 负责“网络前提”
-> HTTP 负责“访问服务”
-> JSON 负责“提取结构化数据”
-> LCD 和日志负责“把结果表达出来”
```

建议后面看代码时按这个顺序理解：

1. 先看 `wifi_service`
2. 再看 `http_service`
3. 再看 `JSON` 解析部分
4. 最后看 `display_service`

这样最容易把这版的主链吃透。
